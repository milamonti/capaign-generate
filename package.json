{
  "name": "fastify-llm-campaign",
  "version": "0.1.0",
  "description": "Backend Fastify que recebe dados de campanha e gera roteiro via LLM",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "NODE_ENV=development nodemon --watch src --exec node src/server.js",
    "test": "node tests/campaign.test.js"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "dotenv": "^16.0.0",
    "fastify": "^5.8.4",
    "pino-pretty": "^13.1.3",
    "undici": "^6.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.0"
  }
}
